; PlatformIO Project Configuration File
; Sistema de Riego MQTT - ESP32 Firmware
; https://docs.platformio.org/page/projectconf.html


[env:nodemcuv2]
platform = espressif8266
board = nodemcuv2
framework = arduino
upload_port = COM3

; Configuración de velocidad
monitor_speed = 115200
upload_speed = 921600

; Librerías requeridas
lib_deps = 
    ; MQTT client
    knolleary/PubSubClient@^2.8
    ; JSON parsing
    bblanchon/ArduinoJson@^6.21.3
    ; NTP time sync
    arduino-libraries/NTPClient@^3.2.1
    ; OLED display SSD1306
    adafruit/Adafruit SSD1306@^2.5.7
    adafruit/Adafruit GFX Library@^1.11.3

; Sistema de archivos
board_build.filesystem = littlefs

; Particiones (para OTA en el futuro)
; board_build.partitions = default.csv

; Flags de compilación
build_flags = 
    -DCORE_DEBUG_LEVEL=3
    -DCONFIG_ARDUHAL_LOG_COLORS

; Excluir archivos de ejemplo de librerías (reducir tamaño)
lib_ldf_mode = deep+

; Monitor serial
monitor_filters = 
    esp32_exception_decoder
    colorize

[env:nodemcuv2_ota]
platform = espressif8266
board = nodemcuv2
framework = arduino
upload_protocol = espota
upload_port = 192.168.10.140
monitor_speed = 115200
upload_speed = 921600
lib_deps = 
    knolleary/PubSubClient@^2.8
    bblanchon/ArduinoJson@^6.21.3
    arduino-libraries/NTPClient@^3.2.1
    adafruit/Adafruit SSD1306@^2.5.7
    adafruit/Adafruit GFX Library@^1.11.3
board_build.filesystem = littlefs
build_flags = 
    -DCORE_DEBUG_LEVEL=3
    -DCONFIG_ARDUHAL_LOG_COLORS
lib_ldf_mode = deep+
monitor_filters = 
    esp32_exception_decoder
    colorize

[env:nodemcuv2_mosfet]
platform = espressif8266
board = nodemcuv2
framework = arduino
upload_port = COM3

; Configuración de velocidad
monitor_speed = 115200
upload_speed = 921600

; Librerías requeridas
lib_deps = 
    knolleary/PubSubClient@^2.8
    bblanchon/ArduinoJson@^6.21.3
    arduino-libraries/NTPClient@^3.2.1
    adafruit/Adafruit SSD1306@^2.5.7
    adafruit/Adafruit GFX Library@^1.11.3

; Sistema de archivos
board_build.filesystem = littlefs

; Flags de compilación
build_flags = 
    -DCORE_DEBUG_LEVEL=3
    -DCONFIG_ARDUHAL_LOG_COLORS
    -DOUTPUT_ACTIVE_HIGH
    
; Excluir archivos de ejemplo de librerías (reducir tamaño)
lib_ldf_mode = deep+

; Monitor serial
monitor_filters = 
    esp32_exception_decoder
    colorize

[env:nodemcuv2_mosfet_ota]
platform = espressif8266
board = nodemcuv2
framework = arduino
upload_protocol = espota

; IP por defecto OTA (puede sobreescribirse por CLI con --upload-port)
upload_port = 192.168.10.140

; Configuración de velocidad
monitor_speed = 115200
upload_speed = 921600

; Librerías requeridas
lib_deps = 
    knolleary/PubSubClient@^2.8
    bblanchon/ArduinoJson@^6.21.3
    arduino-libraries/NTPClient@^3.2.1
    adafruit/Adafruit SSD1306@^2.5.7
    adafruit/Adafruit GFX Library@^1.11.3

; Sistema de archivos
board_build.filesystem = littlefs

; Flags de compilación
build_flags = 
    -DCORE_DEBUG_LEVEL=3
    -DCONFIG_ARDUHAL_LOG_COLORS
    -DOUTPUT_ACTIVE_HIGH
    
; Excluir archivos de ejemplo de librerías (reducir tamaño)
lib_ldf_mode = deep+

; Monitor serial
monitor_filters = 
    esp32_exception_decoder
    colorize

; Configuración avanzada (opcional)
; board_build.mcu = esp32
; board_build.f_cpu = 240000000L
; board_build.f_flash = 80000000L
; board_build.flash_mode = dio
